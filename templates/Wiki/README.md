# Foundry Template Wiki

Adoption copies `MEMORY.md`, `SCHEMA.md`, and `Machine/Foundry Instance.md` into
the instance root's `Wiki/` only when those paths do not already exist. Existing
memory is never overwritten. The template contains no personal data, secrets,
runtime state, or host-specific path.
