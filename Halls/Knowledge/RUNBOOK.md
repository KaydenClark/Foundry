# Knowledge Runbook

S-001 owns the Grounding Journal primitive. An adopting instance must supply
its own accepted private authorization before binding or execution. Verify the
focused behavior and native Hall layout:

```bash
node --test test/grounding-journal.test.mjs
node ../../tools/test-foundry.mjs
```

Focused fixtures prove paired state/event validation, deterministic digest
chains, replay, duplicate handling, ordering, exact recovery, journal-health
Projection, dotted type syntax, and public-field rejection. The integrated disposable Git engine
proves schema-v2 claim, handoff, terminal, denial, repair, and parent events
travel with state in one commit. No live Journal service or store exists.
