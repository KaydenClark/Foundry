# Shipping - Agent Operating Contract

Shipping packages a declared producer into a clean reproducible deployable
product. Shipping is a provisional Hall name and is not a generic release router.

Follow the root `AGENTS.md`; executable capability work requires an approved
Shipping-local spec.
