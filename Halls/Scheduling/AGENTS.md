# Scheduling - Agent Operating Contract

Scheduling makes Orchestration-selected schedules run reliably. It owns timing,
queues, quotas, concurrency, and wakeups, not routing or assignment.

Follow the root `AGENTS.md`; executable capability work requires an approved
Scheduling-local spec.
