# Halls and Modules are ordinary workspace-tracked source. Registered component
# worktrees and deployment-local state are not.
/.worktrees/
/.local/
/.foundry/

# Secrets, runtime output, and editor/OS noise never belong in the harness.
.env
.env.*
!.env.example
*.log
.DS_Store
node_modules/
coverage/
dist/
